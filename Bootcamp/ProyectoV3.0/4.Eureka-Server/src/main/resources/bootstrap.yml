server:
  port: 8761

spring:
  application:
    name: discovery-server

  cloud:
    config:
      uri: http://localhost:8084
      username: root
      password: root

  eureka:
    client:
      registerWithEureka: false
      fetchRegistry: false