spring:
  application:
    name: registry-service

  eureka:
    client:
      registerWithEureka: false
      fetchRegistry: false

  cloud:
    config:
      uri: http://localhost:8084
      username: root
      password: root