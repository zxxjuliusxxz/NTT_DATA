server:
  port: 8086
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zxxjuliusxxz/NTT_DATA/tree/main/Bootcamp/ProyectoV1.0
          default-label: main
          search-paths: configdata
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}
  security:
      user:
        name: root
        password: root
  application:
    name: config-services