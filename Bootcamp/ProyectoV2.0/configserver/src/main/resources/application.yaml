server:
  port: 8084

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zxxjuliusxxz/NTT_DATA.git
          default-label: main
          search-paths: configdata
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}
  security:
      user:
        name: root
        password: root